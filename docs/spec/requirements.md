# Requirements — kmixdeck

Numbered, testable requirements. `MUST` / `SHOULD` / `MAY` as in RFC 2119.
Each requirement carries a source: `[owner]` = project owner's stated need,
`[wavelink #n]` = feature n in `docs/research/wavelink-feature-inventory.md` (Ln = documented limitation), `[users]` = recurring user feedback in `docs/research/user-feedback.md`,
`[platform]` = required by PipeWire/KDE integration.

Status legend: 📝 draft · 🔶 partly covered · ✅ **verified by an automated test in this repo** (VF-2; "agreed" alone is not a status — either it is tested or it is a draft)

**Tiers (ADR 0010 D3, AR-12):** a row tagged `tier:core` is ✅ only with a proof in every frontend (CLI + KDE window + tray) in
`tests/integration/frontends_sync suite`; untagged rows are `full` (CLI + window) or `api` (CLI) as their text says.

**Opt-in rule (owner 2026-09-18):** every feature added after v0.2 that changes the audio path or adds UI surface (FX-8, FX-9, FX-10, CT-8, CT-9, UX-18) MUST be **off/absent by default and switchable at runtime** without restart; the UI MUST hide the controls of a disabled feature except for the one switch that enables it. Exception: a default that is clearly the sensible one for the created object (limiter and LUFS on the Stream mix from the first-run wizard) MAY start on — the switch stays.

**Status:** 104 ✅ · 1 🔶 · 6 📝 (111 rows) — this line is written by `tools/sot-audit.py --write` and checked by the
ctest `sot-audit`, which also fails the build when a ✅ row names no existing test.

---

## 1. Channels (inputs)

| ID | Requirement | Source | Status |
|---|---|---|---|
| CH-1 | The app MUST provide virtual audio channels (e.g. *Game*, *System*, *Voice*, *Music*, *Browser*) that appear to the desktop as ordinary output devices, selectable in any application and in the Plasma volume applet. | owner, wavelink | ✅ starter layout + `channel add`; test_add_channel_creates_sink_cells_and_persists, test_fader_in_one_mix_never_leaks_into_the_other (mic → voice) |
| CH-2 | Channels MUST be user-definable: create, rename, reorder, delete, choose icon/colour. No fixed set, no fixed count. | owner | ✅ create/rename/delete via bus, CLI and UI (⋮ menu / right-click on every row and column header, RenameDialog); slug and node names are stable across rename. test_add_channel_creates_sink_cells_and_persists |
| CH-3 | Physical inputs (microphones, capture cards, line-in, Bluetooth) MUST be usable as channels alongside virtual ones — as `Input` objects attached to a channel (ADR 0007 D2). | wavelink; ADR 0007 | ✅ test_ch3_* — `kmixdeck channel input voice fake.mic`, `Channel.InputDevice`, `kmixdeck.in.voice` in graph |
| CH-4 | An application MUST be assignable to a channel from within the app (drag-and-drop or picker); the assignment MUST persist across app restarts, PipeWire restarts and reboots. | owner, wavelink; tests `test_ch4_move_app_to_channel_is_immediate_and_audible`, `test_ch4_routing_survives_app_restart`, `test_ch4_routing_survives_pipewire_restart` (WirePlumber restore-target, keyed by node.name) | ✅ |
| CH-5 | New, never-seen applications MUST land on a user-chosen default channel (default: *System*). | owner | ✅ `Mixer.DefaultChannel` (o, "/" = off), `kmixdeck channel default [slug|none]`, checkable "New applications start here" in the row menu; only never-routed apps are moved (knownApps in layout.json), user moves win (WirePlumber restore-target). test_ch5_default_survives_restart_and_dies_with_its_channel::test_ch5_* (3) + test_ch10 |
| CH-6 | Assignment MUST survive PipeWire renaming or re-creating an app's node (see Sonusmix #38); matching MUST NOT rely on volatile node IDs alone. | platform | ✅ layout keys on application.name (never the node id); proven for a restart with a new id, a NEW node.name, and a new node.name + media.name — channels AND the CH-12 relay follow (`test_ch6_assignment_survives_node_id_name_and_metadata_churn`). Found on the way: the relay was built for the OLD node.name after a rename → second channel silent; the layout entry now follows the node |
| CH-7 | Each channel MUST have: mute, gain trim, level meter (peak + RMS), clip indicator. `tier:core` | wavelink | ✅ trim + mute (`test_channel_trim_and_mute_hit_every_mix`), trim dial in the window + slider in the tray (`test_core_feature_reaches_cli_window_and_tray`); peak meter (UX-6) + RMS core + clip cap, all three computed once in the daemon (`rms/<key>`, `clip/<key>` on the Levels signal, clip held ≈ 1.5 s) and shown in window, tray and CLI (`test_ch7_rms_and_clip_are_metered_in_the_daemon_and_shown_everywhere`) |
| CH-8 | Channels MAY be grouped/linked so one fader moves several channels. | wavelink | ✅ `Channel.Group` (s, "" = none): channels sharing a group move together — a trim change is applied as the same dB delta to every member (their balance survives; from silence: absolute), mute is mirrored; no master, touch any member. CLI `channel group <slug> [<name>|none]`, `channel groups`; window: ⛓-badge on every member + Group submenu (join existing / new / leave), a dial move on one member moves the others in the daemon; tray: ⛓ after the name. Persists in layout.json. `test_ch8_channel_groups_move_together_in_cli_window_and_tray`. Screenshot docs/screenshots/channel-groups.png |
| CH-9 | Deleting a channel or mix MUST be undoable (Wave Link 3.2 added undo after user complaints). | wavelink #12 | ✅ `Mixer.Undo()` + `UndoDescription`, `kmixdeck undo`, toast "Removed channel “Voice” — Undo" + Ctrl+Z. Restores layout entry, links, hardware input, default-channel flag, outputs/fallback, master AND every cell fader/mute (applied as the loopbacks reappear). One level; a new add clears it. test_ch9_undo_restores_channel_with_faders_links_input_and_default::test_ch9_* (2), UI click-verified |
| CH-10 | The app picker MUST group applications by category and show which channel each running app is on; apps without a recognisable PipeWire node (Sonusmix #37: mpv) MUST still be listed via their client/application name. | wavelink #10, users; test `test_ch10_running_apps_are_listed_with_their_channel`; `org.kmixdeck1.App` objects; `kmixdeck app list` | ✅ |
| CH-11 | Unused/hidden physical devices SHOULD be hideable from the channel list without deleting them. | wavelink #14 | ✅ `Mixer.HiddenDevices` + `SetDeviceHidden(node, bool)`; CLI `devices hide|unhide|hidden`; window: right-click a patchbay device card → hide, drawer → "Hidden devices…" → show again. Hidden = out of every picker (listening box, mix Outputs, channel Inputs, AddDialog, patchbay) but still a device, still routable by name, and visible where it is in use; survives a restart (`test_ch11_hidden_device_leaves_every_picker_stays_routable_and_comes_back`). tier:full — the tray's only picker reads the same filtered list |
| CH-12 | An application MAY be assigned to **more than one channel at once** (e.g. mic → Voice *and* Stream); every assigned channel hears it, the assignment persists like CH-4. | owner 2026-09-15 | ✅ `App.Channels` (as, [0]=primary via WirePlumber restore-target, rest via relay loopbacks capturing the app node), `App.Assign(as,b)`, `kmixdeck app assign <app> <ch>,<ch>`; layout.json `apps[]`; test_ch12_multi_assign_relays_only_that_app::test_ch12_multi_assign_relays_only_that_app (relay carries ONLY that app, survives daemon restart) |
| CH-13 | Adding a channel MUST offer every possible source in ONE picker: running application streams (icon, name, live level), hardware inputs (mics, capture devices, channel subsets per DV-13) and "empty (apps only)". Choosing an app creates the channel AND assigns the app (CH-4); choosing a device creates the channel AND its input edge (DV-9). | owner 2026-09-16 ("beim Hinzufügen alle Apps zeigen") | ✅ `AddDialog`: apps (icon, live level, current channel) + inputs + empty; `MixerClient.addChannelWithSource`; test `test_ch13_add_channel_from_source_app_and_device`; screenshot 2026-09-16 |
## 2. Mixes (outputs)

| ID | Requirement | Source | Status |
|---|---|---|---|
| MX-1 | The app MUST support **any number of output mixes** (1…n), added with a `+` action and removed individually. There MUST NOT be a hard-coded maximum. | owner; tests `test_mx1_add_mix_at_runtime_creates_cells_and_persists`, `test_mx1_remove_mix` (3rd mix → 9 cells, removal → 6) | ✅ |
| MX-2a | Mute MUST be per cell (channel × mix): muting *Game* in *Stream* MUST NOT affect *Game* in *Monitor* nor *System* in *Stream*. | owner; test `test_ch4_mute_is_per_cell` | ✅ |
| MX-2 | Every mix MUST have its own fader (and mute) **per channel**. Changing *Game* in mix *Monitor* MUST NOT change *Game* in mix *Stream*. | owner; test `test_mx2_per_mix_level_is_independent`, `test_mx2_other_direction` (−12.0 dB / +6.0 dB measured) | ✅ Cell state is guarded against foreign writers (WirePlumber restore-stream, pw-cli): the daemon keeps the user's intent per node and undoes any echo that disagrees, at any time — `test_mx2_cell_state_survives_a_foreign_writer`. |
| MX-3 | A mix MUST be routable to (a) a physical output device (headphones, speakers), (b) a virtual capture device that other software (OBS, Discord, a recorder) can pick as its input, or (c) both. | owner, wavelink; (b) covered by `test_graph_comes_up_from_config_alone` (`kmixdeck.source.stream`), (a)/(c) open; tests `test_mx3a_mix_output_follows_device_and_is_audible` (tone measured on the device), `test_mx3a_output_none_unlinks_and_unknown_device_is_rejected`, `test_mx3a_output_device_persists_in_generated_conf`; every mix also exposes `kmixdeck.source.<slug>` | ✅ |
| MX-4 | Default setup on first run SHOULD create two mixes: *Monitor* → default output device, *Stream* → virtual capture device. | owner | ✅ Layout::starter(): Monitor (no output until chosen — the default sink is never auto-picked, MX-3a) + Stream (capture-only, `kmixdeck.source.stream`); test_dv7_levels_and_mute_survive_daemon_restart, test_rename_mix_keeps_routing_and_output::test_rebuild_from_empty_and_default_layout_on_missing_file |
| MX-5 | Mixes MUST be nameable, reorderable, colour-coded; the UI MUST scale to ≥ 8 mixes without hiding faders. `tier:core` | owner | ✅ name (UX-8), order (UX-9), colour: `Mix.Color`/`Channel.Color` = #rrggbb or "" (daemon validates), CLI `mix|channel color <slug> <#rrggbb|none>`, window: colour submenu + stripe on the header card, tray: stripe on the row; same 8-colour palette everywhere (`MixerClient.colorPalette`). Core-tier proven CLI→window→tray (`test_core_feature_reaches_cli_window_and_tray[MX-5]`). ≥ 8 mixes: the mix strip scrolls horizontally, every master and cell fader keeps its size and is on the page (`test_mx5_eight_mixes_all_faders_visible_and_bad_colour_refused`, docs/screenshots/eight-mixes.png) Columns share the width instead of scrolling (Michel 2026-09-17 "Scrollbalken? Mix 2 3 4 5 6 ??"): headers fold to two rows below 14 gridUnits, six mixes fit at 1280 px, eight at 1600 px; below 8.5 gridUnits per mix the strip scrolls so the master fader stays grabbable. Screenshots six-mixes-1280.png, eight-mixes-1600.png. |
| MX-6 | Each mix MUST have a master fader, mute and meter. `tier:core` | wavelink | ✅ Mix.Volume/Muted/ToggleMute on the bus, `kmixdeck mix volume|mute`, master slider + mute in the column header; test_output_device_receives_exactly_the_mix::test_mix_master_* (−20 dB reaches device, mute silences, other mix untouched) |
| MX-7 | Per-channel fader per mix MUST include a *link to another mix* toggle (e.g. "Stream follows Monitor for this channel") that can be broken at any time. | wavelink | ✅ `Cell.Follows` (o, "/" = independent), `kmixdeck cell link <ch> <mix> <other|none>`, link button in every cell (auto-picks the other mix when there are two). Volume+mute mirrored, external moves (Plasma applet) propagate, ANY direct write to the follower breaks the link, no A↔B loops, survives restart. test_mx7_cell_link_stream_follows_monitor_and_breaks_when_touched::test_mx7_* (acoustic) |
| MX-8 | A mix MAY be duplicated as a starting point for a new mix. | owner | ✅ `Mixer.DuplicateMix(source, name)`: icon, colour, FX chain, master level/mute and every cell fader/mute are copied, outputs are not; CLI `mix duplicate <slug> <name>`, window: header menu → Duplicate… (name dialog). Measured on the copy: −18 dB (cell −12 + master −6) under the channel, muted cell stays muted (`test_mx8_duplicate_mix_copies_levels_fx_colour_not_outputs_from_cli_and_window`). Found on the way: WirePlumber writes its default volume to a node it has never seen right after ours → levels for brand-new nodes are re-applied once after 400 ms |
| MX-9 | A mix MUST be sendable to several hardware outputs at once (e.g. headphones + speakers) — `Mix.Outputs` list, one loopback per output (ADR 0007 D2). | wavelink #5; ADR 0007 | ✅ `Mix.Outputs` (as) + `AddOutput`/`RemoveOutput`, `kmixdeck mix outputs|output-add|output-remove`; header menu is multi-select ("2 outputs"). test_mx9_mix_plays_to_several_outputs_and_dv15_fallback_takes_over::test_mx9_*: both devices read the same level; OutputDevice replaces only Outputs[0] |
| MX-10 | Muted mixes MUST be unmistakable in the UI (Wave Link: header turns red). `tier:core` | wavelink #8 | ✅ `MixHeader.qml` negative background + "— muted" title; proven by `test_mx10_muted_mix_header_is_red_and_says_so` (reads the rendered colour via `kmixdeck-kde --probe`, other mix unchanged, red-dominant) |

## 3. Microphone & effects

| ID | Requirement | Source | Status |
|---|---|---|---|
| FX-1 | Any channel MUST accept an ordered effects chain (insert). Minimum built-in set: noise suppression, noise gate, compressor, EQ, high-pass, limiter. | wavelink | ✅ `kmixdeck fx set/get/control`, D-Bus `Fx` property + `SetFxControl`; filter-chain per channel/mix; 4/4 tests no xfail (ADR 0008 resolved 2026-09-16) — `test_fx_chain_set_get_and_live_control`, `test_fx_changes_the_sound_and_bypass_restores_it`, `test_fx_types_and_presets_are_on_the_bus` Web: `test_fx8_web_ui_edits_the_chain_the_window_and_cli_see`.; `tests/unit/fxtest.cpp` (no LADSPA needed) |
| FX-2 | Effects MUST be implemented with open plugin standards available on Linux (LV2/LADSPA) through PipeWire filter-chain; the user MAY add any installed LV2 plugin. | platform | ✅ LADSPA via PipeWire `filter-chain` (swh-plugins: gate/compressor/eq/limiter/highpass); `FxTypes` on the bus; `test_fx_types_and_presets_are_on_the_bus` |
| FX-3 | Effects on a channel apply before the mix faders, so every mix hears the processed signal. | wavelink | ✅ chain sits on `kmixdeck.fx.<ch>` in front of the channel sink → every cell hears the processed signal; `test_fx_changes_the_sound_and_bypass_restores_it` (gate closes → all mixes −∞) Web: `test_fx8_web_ui_edits_the_chain_the_window_and_cli_see`. |
| FX-4 | The mic channel SHOULD offer one-click presets (e.g. "Voice — clean", "Voice — broadcast") that the user can edit and save. | users | ✅ `FxPresets` (Voice — clean / broadcast …), `kmixdeck fx presets`, applied via `fx set`; `test_fx_types_and_presets_are_on_the_bus`; `tests/unit/fxtest.cpp` (no LADSPA needed) |
| FX-5 | Effects MUST be bypassable per effect and per chain without audio dropouts. | wavelink | ✅ per-effect `enabled` + chain `enabled`; live controls via Props (no rebuild); chain swap re-targets streams (`retargetWhenPresent`) — `test_fx_changes_the_sound_and_bypass_restores_it` Web: `test_fx8_web_ui_edits_the_chain_the_window_and_cli_see`.; `tests/unit/fxtest.cpp` (no LADSPA needed) |
| FX-6 | Effects MUST be available on mixes (output) as well as on channels (input) — e.g. a limiter on the Stream mix. | wavelink #37 | ✅ `fx set mix <slug>` — same chain model on mix sinks (`kmixdeck.fx.mix.<slug>`); `test_fx_chain_set_get_and_live_control` covers mix path |
| FX-7 | Effect chains MUST be copyable between channels, including from channels whose device is currently absent. | wavelink #35 | ✅ chain is plain JSON on the bus: `fx get channel a | fx set channel b` — device absence irrelevant (layout only); `test_fx7_chain_copies_between_channels_even_with_absent_device` (unplugged input, daemon restart); `tests/unit/fxtest.cpp` (no LADSPA needed) |
| FX-8 | **Noise suppression (Wave Link "Voice Focus").** A channel MAY carry a noise-suppression stage as an ordinary FX-1 effect, built on a free plugin shipped by the distro (`rnnoise` LADSPA/LV2, `noise-suppression-for-voice`), inserted through the same PipeWire `filter-chain` as every other effect. The preset "Voice — clean" (FX-4) MUST include it **switched off**; enabling is one toggle. When the plugin is not installed the effect MUST be listed as unavailable with the package name, never fail silently. `tier:full` | owner 2026-09-18, wavelink #3 | 📝 |
| FX-9 | **Ducking (side-chain).** A channel MAY be ducked by another channel: when the trigger channel (mic) carries signal, the ducked channel (music) drops by a user-set depth (dB) with attack/release. Implemented as one `filter-chain` with two inputs (LSP `sc_compressor` or equivalent free LADSPA/LV2 side-chain compressor), the trigger fed from the trigger channel's post-FX node. **Off by default**, one pair at a time is the minimum. The ducked channel MUST show a "ducked by <channel>" badge and the current reduction on its meter. `tier:full` | owner 2026-09-18, wavelink (Wave XLR Pro exclusive) | 📝 |
| FX-10 | **Broadcast-safe limiter on mixes.** Every mix MAY enable a brick-wall limiter as its last mix effect (FX-6) with a ceiling in dBTP (default −1 dBTP). Default **off**; the Stream mix created by UX-3 SHOULD have it on. Gain-reduction MUST be visible on the mix meter. This is Elgato's "Clipguard" done in software; it MUST NOT be reachable by any channel effect (channels clip before the mix, the limiter is a mix property). `tier:full` | owner 2026-09-18, wavelink #7 | 📝 |

## 4. Control & integration

| ID | Requirement | Source | Status |
|---|---|---|---|
| CT-1 | Global shortcuts (mute mic, mute channel X in mix Y, volume up/down, switch monitoring mix) MUST work on Wayland, via KGlobalAccel. | owner, platform; KGlobalAccel actions per channel (`mute-channel-<slug>`) and per mix (`mute-mix-<slug>`), component `kmixdeck`, assignable in System Settings → Shortcuts; `Cell.ToggleMute`/`Channel.ToggleMute` on the bus; tests `test_ct1_toggle_mute_is_atomic_on_the_bus`, `test_ct1_kde_frontend_registers_global_shortcuts` — volume-up/down per mix and 'listen-next-mix' registered and exercised (`test_ct1_kde_frontend_registers_global_shortcuts`); on Plasma 6.7/Wayland: component registered, `invokeShortcut` via kglobalacceld mutes through the daemon (2026-09-15); left: ONE physical keypress on real hardware confirmed by the owner (System Settings → Shortcuts → kmixdeck → assign, press, hear it) — not testable from here | 🔶 |
| CT-2 | A documented local control API (D-Bus and/or a small IPC protocol) MUST expose every fader, mute and mix switch so external controllers can drive the app. | owner, wavelink; = the D-Bus API (ADR 0005), 100 % covered by `kmixdeck` CLI; `test_ar2_third_party_client_needs_none_of_our_code` | ✅ |
| CT-3 | Stream Deck support MUST be provided on top of CT-2 — via an existing Linux Stream Deck host (OpenDeck / streamdeck-ui plugin) rather than our own HID stack. | wavelink; not a need of the project owner (no Stream Deck in his setup, scene switching runs via HA) — kept for users coming from Wave Link, cost was ~250 lines of Python on top of CT-2 | ✅ OpenAction plugin `streamdeck/me.kmixdeck.sdPlugin` (OpenDeck / Tacto / Stream Deck SDK subset): Mute, Fader (key ± dB; dial rotate/press), Listen-on. No logic in the plugin — one CLI call per press, titles/states from `kmixdeck watch`; property inspector lists channels/mixes/cells/devices live from the daemon. `kmixdeck streamdeck install|uninstall|path`. Proven against a fake OpenAction server + real daemon in `test_ct3_stream_deck_plugin_drives_and_mirrors_the_daemon` (both directions: deck → daemon, daemon → deck); manifest checked against OpenDeck's serde rules. Real hardware pending (no OpenDeck on the dev box). streamdeck/README.md, docs/screenshots/streamdeck-property-inspector.png |
| CT-4 | The app MUST integrate with the Plasma system tray (StatusNotifierItem): quick mute, mix switch, level indication. | owner; KStatusNotifierItem with per-channel mute toggles, per-mix mute, icon reflects mute state, closing the window keeps the tray; level indication still missing; registered with the real `StatusNotifierWatcher` on Plasma 6.7 (2026-09-15); level indication still missing | ✅ KStatusNotifierItem: click → overview popover with live meters per mix and channel (UX-17), middle click → mute the listening mix, context menu with per-channel/per-mix mute and the listening-mix switch, icon reflects mute state, window close hides to tray. `test_tray_click_opens_overview_double_click_opens_window`, `test_core_feature_reaches_cli_window_and_tray`, `test_ct4_tray_menu_offers_quick_mute_and_mix_switch` |
| CT-5 | Volumes shown in kmixdeck and in the Plasma volume applet MUST agree (no two truths). | platform | ✅ against pipewire-pulse (what plasma-pa talks to): a −12 dB trim in kmixdeck is 63 % / −12.0 dB in the applet, mix mute shows as sink mute, and the other way: the applet at 50 % is trim 0.125 = −18.1 dB in kmixdeck, the applet's mute is Channel.Muted — one cubic scale, no second truth; a daemon restart re-applies the same numbers (`test_ct5_kmixdeck_and_the_pulse_world_show_one_truth`) |
| CT-6 | OBS SHOULD see each mix as a cleanly named capture device, and MAY additionally see each channel separately (for multi-track recording). | owner; `kmixdeck.source.<mix>` Audio/Source per mix, description 'kmixdeck <Mix> Mix' (generated conf + reconcile); test `test_mx3a_output_device_persists_in_generated_conf`; per-channel capture (MAY) not done ; OBS 'Stream Mix (kmixdeck)' input on the laptop, tone level in == OBS meter level | ✅ |
| CT-7 | Settings backup/restore and export MUST be available; a corrupt entry MUST NOT invalidate the whole config (Wave Link 3.3 fixed exactly this). | wavelink #24, L9 | ✅ corrupt layout.json → daemon starts and rebuilds (`test_corrupt_layout_file_does_not_take_the_daemon_down`); backup/restore: `Mixer.Export()`/`Import(json)` = layout + every fader/trim/mute/pan; CLI `export [file]` / `import <file>`, window menu → file dialog (tier:full — a file dialog has no place in the tray). Round trip CLI-export → wreck → window-import proven by measurement (−9 dB cell and −6 dB master back at the mix, the muted cell muted, app on two channels, pan −0.5); garbage and foreign JSON refused without touching anything (`test_ct7_export_import_round_trip_cli_and_window_and_garbage_is_refused`). Found on the way: restoring a channel level after undo/import dropped its pan Web: `test_ct7_web_export_downloads_and_import_round_trips_and_refuses_garbage`. |
| CT-8 | **Soundboard.** A soundboard is a channel of kind `soundboard` that plays user-registered samples (wav/flac/ogg/mp3 via the distro's decoders) into the graph on demand: `Mixer.PlaySample(name)`, `StopSample`, `Samples` (a{sv}: name, path, length, gain), `kmixdeck sample play <name>`. Samples play through the channel's FX chain and per-mix faders like any source, so "mic only" mixes can exclude them. Playback runs inside the daemon (`pw_stream`), not in a spawned process. Triggerable from any CT-2 client (Home Assistant, Stream Deck via CT-3). **The channel kind is optional**: no soundboard channel exists until the user adds one. `tier:core` | owner 2026-09-18, wavelink #5 | 📝 |
| CT-9 | **Scenes (named snapshots).** A scene is a named, saved subset of the layout: per-cell faders and mutes, mix masters and mutes, listening device, FX bypass states — **not** the channel/mix set itself and not the wiring. `Mixer.Scenes`, `SaveScene(name)`, `RecallScene(name)`, `DeleteScene`, signal `SceneRecalled`; `kmixdeck scene save|recall|list|delete`. Recall MUST apply atomically in one graph pass and be undoable (CH-9). Faders move visibly in every frontend. Scenes are files under the layout dir (DV-5), export/import (CT-7) carries them. `tier:core` | owner 2026-09-18; not in Wave Link | 📝 |

## 5. Devices & audio behaviour

| ID | Requirement | Source | Status |
|---|---|---|---|
| DV-1 | The app MUST NOT sit in the audio path as a process. Audio MUST keep flowing when the UI is closed or crashes. | owner; test `test_graph_comes_up_from_config_alone` (graph from config, no app process); test `test_dv1_layout_survives_without_the_daemon` (daemon killed, PipeWire restarted, graph still there from generated conf; reconcile creates nothing twice) | ✅ Web: `test_dv1_web_input_picker_and_ux8_icon_from_the_browser`. |
| DV-2 | Added latency per hop (channel → mix → device) MUST be ≤ one PipeWire quantum at the session's rate; the design MUST avoid unnecessary resampling. | owner; test `test_cell_nodes_run_in_one_graph_cycle` (all nodes RUNNING in one graph cycle, DSP 1–3 µs) | ✅ |
| DV-3 | Hot-plug: when the monitor output device disappears (headset unplugged, Bluetooth drops), the mix MUST fall back to a user-defined device and return automatically when it reappears. | users | ✅ test_dv9_dv12_*: output unplugged → parked on kmixdeck.null, resumes on replug; test_parked_mix_plays_nothing_on_the_default_sink: parked = silent on default sink |
| DV-4 | Sample rate and quantum SHOULD follow the PipeWire session; the app MUST NOT force its own rate. | platform | ✅ a 44.1 kHz / quantum-256 session stays 44.1/256 after kmixdeckd joins; no clock.force-* in the settings metadata, nothing the daemon writes names a rate, every kmixdeck node negotiates the graph rate, meters and audio work at that rate (`test_dv4_daemon_follows_the_sessions_rate_and_quantum_and_forces_nothing`) |
| DV-5 | Configuration MUST be plain files under `$XDG_CONFIG_HOME`, human-readable, diff-able, and MUST restore the full graph on login without user action. | owner; `layout.json` + generated `pipewire.conf.d/90-kmixdeck.conf` on every edit (QSaveFile, atomic) | ✅ `test_dv1_layout_survives_without_the_daemon` (plain `layout.json` + generated conf, edited without the daemon, restored on start); `test_mx3a_output_device_persists_in_generated_conf`; AR-10 hotplug test steps 4–6 (restore on start without user action) |
| DV-6 | Sleep/wake and device re-enumeration MUST NOT lose routing or require a restart (Wave Link 3.x release notes list repeated fixes here; VoiceMeeter forum: crackling after updates). | wavelink #44, users | ✅ simulated as PipeWire sees a suspend: speakers, headphones and the 4-in interface vanish at once, return with new ids in another order — listening device, both mix outputs, the port wire, cell fader, channel mute and the app assignment are all still there, audio input→channel→mix→speakers is back 1.7 s after the devices return at the same level, same daemon pid (`test_dv6_sleep_wake_every_device_gone_and_back_routing_intact_no_restart`). Real S3 on real hardware: to be observed once, not testable from here |
| DV-8 | Frontends MUST be able to list the hardware outputs a mix can play to, without talking to PipeWire themselves. | owner 2026-09-14; `Mixer.OutputDevices` (a{ss}), `kmixdeck devices`, mix-header menu; test `test_dv8_devices_lists_hardware_sinks_not_ours` | ✅ |
| DV-9 | A device reference MUST be the PipeWire `node.name` (stable across replug/reboot, same key WirePlumber uses); node ids/serials/bus paths MUST NOT be persisted. | ADR 0007 D1 | ✅ test_dv9_dv12_* — reference is `node.name`, survives absence |
| DV-10 | Input devices (`Audio/Source`: mics, capture cards, BT) MUST attach to a channel; output devices (`Audio/Sink`) MUST attach to a mix; the UI MUST offer each kind only where it fits. | ADR 0007 D2 | ✅ `Mixer.InputDevices` (Audio/Source) on Channel, `Mixer.OutputDevices` (Audio/Sink) on Mix; UI menus offer only the fitting kind — `test_ch3_devices_in_lists_sources_and_channel_input_is_settable`, `test_dv8_devices_lists_hardware_sinks_not_ours` |
| DV-11 | An absent (unplugged) input or output MUST stay configured, be shown greyed with its last-seen name, and MUST NOT be deleted automatically. `tier:core` | ADR 0007 D3; wavelink L7/#44 | ✅ test_dv9_dv12_* — `InputPresent`/`OutputPresent` false, value kept; UI shows "(unplugged)" dimmed |
| DV-12 | When an absent device reappears, its routing MUST be restored by the daemon within 2 s with no client action; while absent, its loopback MUST be parked on `kmixdeck.null`, never on the default device. | ADR 0007 D3 | ✅ test_dv9_dv12_* — parks on `kmixdeck.null`, resumes on replug w/o client action |
| DV-13 | Multi-channel devices (RØDECaster Pro II, Ui24R with 32 in / 32 out over USB): a device reference MUST be able to select a **channel subset** — one port (mono) or a pair (stereo) — not only the whole device. Device lists MUST expose channel count and port positions so the picker can offer "CH 18–19 (stereo)" / "CH 21 (mono)". | ADR 0007 D4 | ✅ `Mixer.DevicePorts` (a{sas}, node → `POS\|port.name\|alias`), `kmixdeck devices ports <node>` incl. "in use by"; `test_dv20_daemon_publishes_ports_of_a_device` |
| DV-17 | **Virtual device from ports (input side):** a channel MAY be fed by an arbitrary subset of one device's *capture* ports — mono (1 port) or stereo (2 ports, L/R chosen freely, e.g. Ui24R AUX 3 + AUX 4, or RØDECaster "Mic 1" alone). The result behaves like any other channel: fader per mix, meters, effects, listen. Several channels MAY use different ports of the same device at the same time (Ui24R → 4 channels from CH 18/19/20/21). | owner 2026-09-16 ("Dual-Channel oder Mono auf ein virtuelles Device") | ✅ ref `node:POS[,POS]` on `Channel.InputDevice`; measured `test_dv17_two_mono_channels_from_one_device_hear_only_their_port` (mono + stereo pair, sides kept) |
| DV-18 | **Virtual device from ports (output side):** a mix output MAY target a subset of one device's *playback* ports — mono or stereo pair — so that e.g. Monitor → RØDECaster "Headphones 1" (2 ports) and Stream → Ui24R "USB return 5–6" coexist on the same hardware. Unselected ports of the device are left untouched (other software may use them). | owner 2026-09-16 | ✅ same ref on `Mix.OutputDevice`/`AddOutput`; `test_dv18_mix_output_into_a_port_subset_leaves_other_ports_silent` (AUX3/4 hot, AUX1/2 −inf, survives restart) |
| DV-19 | Port-based virtual devices MUST appear to the desktop as ordinary sinks/sources with a user-chosen name and icon ("Talkback", "Stream Return") — usable from Plasma, OBS, Discord like any device (same contract as CH-1). Mono ↔ stereo adaptation (mono source into a stereo channel, stereo mix into a mono port) MUST be explicit in the picker and MUST NOT silently drop a side. | owner 2026-09-16 | ✅ mono → [MONO] playback (both sides, equal), bad port / 3 ports rejected on bus AND CLI; `test_dv19_mono_input_reaches_both_sides_and_bad_port_is_rejected` |
| DV-20 | The picker (CH-13) and the routing view (UX-15) MUST show ports the same way: device → ports with position labels from PipeWire (`audio.position`, `port.alias`) and, when the device names them (RØDECaster: Mic 1–4, Bluetooth, USB 1/2; Ui24R: CH 1–32, AUX 1–10), that name. Selected ports are marked "in use by <channel/mix>" so the same port is not routed twice by accident (allowed, but visible). | owner 2026-09-16 | ✅ `test_dv20_daemon_publishes_ports_of_a_device` (DevicePorts = position + PipeWire label, monitor ports excluded); picker and routing view read the same `Mixer.devicePorts`; patchbay shows "→ <channel>" per used connector (DV-24) |
| DV-21 | **Per-side routing (Michel 2026-09-16):** the left and right side of a stereo channel/mix MUST be addressable individually where it makes sense: a channel MAY take its L and R from ports of *different* devices; a mix output MAY send only L or only R to a port; the routing view MUST let the user connect single sides (L/R handles on the source and target boxes), not just whole pairs. | ✅ ref `node:POS>L|R` on inputs (port → one side) and outputs (one side of the mix → port); routing view L/R pads on channel + output boxes, edges aim at the pad; `test_dv21_side_bound_input…`, `test_dv21_side_bound_output…`. Two device ports → one side is refused (PipeWire cannot fold, measured; ADR 0009 A2) |
| DV-22 | **Mono ↔ stereo is a property of the edge, never silent:** a mono port feeding a stereo channel goes to *both* sides by default (DV-19), but the user MUST be able to choose "left only" / "right only" instead; a stereo source into a mono port MUST sum L+R (−3 dB) — the picker shows the chosen conversion in words. | ✅ mono→centre by default (DV-19); `Channel.Pan` (−1..1, persisted in `layout.json`, re-applied when the node returns), `kmixdeck channel pan <slug> [<-1..1>|L|C|R]`, dial in `ChannelHeader.qml` (double-click = centre, L40/C/R100 label). Balance law: panned-to side stays at trim, other side cos-fades. `test_dv22_pan_moves_a_mono_source_between_the_sides_and_persists` (centre equal, hard L/R other side silent, −0.5 ≈ −3 dB, survives restart) |
| DV-23 | **Virtual test device:** `kmixdeckd` MUST be able to create a persistent virtual multichannel device (default: 8 inputs + 8 outputs named like a Ui24R, AUX1..AUX8) via `kmixdeck devices virtual add <name> [--in N] [--out N]`, so port routing can be exercised without hardware; it is a real PipeWire node (apps can play into its outputs, its inputs appear in the picker) and survives daemon restarts (layout). | ✅ `devices virtual add|remove|list`, `Mixer.AddVirtualDevice`, layout-persistent, real ports (`test_dv23_…`); render-ui uses it |
| DV-24 | **Patchbay view, Loopback-style (Michel 2026-09-16, reference: rogueamoeba.com/loopback):** three columns **Sources → Channels → Outputs**. Every source (app, hardware device, virtual device) is ONE card: title, on/off, and one row per connector (`1 (L)`, `2 (R)`; a Ui24R shows all 32 rows, collapsible) — each row has a live level bar and a **jack on the right edge**. Channels and outputs are cards whose rows (`L`, `R`) have a **jack on the left edge** (and on the right for channels, so they can feed outputs). Wires are drawn as smooth S-curves jack→jack in the accent colour; drag from a jack to a jack creates a wire, click a wire removes it. | ✅ `PatchBayPage.qml` (2026-09-16): cards per device/channel/mix with one row per connector, jacks on the card edge, wires as S-curves ending on jacks; drag jack→jack wires (`MixerClient.connectJacks`), click on a wire removes it (`removeWire`); big cards collapse to wired connectors; shared `patchbay()` view model (AR-9); gestures proven at the daemon by `test_dv24_patchbay_gestures_reach_the_daemon` |
| DV-25 | **Wires are refs.** One wire = one `node:POS>L|R` (or `node:POS,POS` for a pair drawn together); a channel MAY carry several wires from different connectors and devices (one edge per wire); a mix output likewise. CLI, layout and patchbay describe the same set of wires — `kmixdeck wires` lists them, `kmixdeck wire add/remove <from> <to>` edits them. | ✅ `Channel.Inputs` (as) + `AddInput`/`RemoveInput`; CLI `channel inputs|input-add|input-remove`; one loopback per wire, primary promotion, restart-safe; every wire in the patchbay is such a ref (`test_dv25_several_wires_into_one_channel_from_two_devices`) |
| DV-26 | **Device card = the real device.** Connector names come from the device (RØDECaster: Mic 1–4, BT, USB 1/2; Ui24R: 1–32); "in use by …" on every connector; unplugged devices stay as greyed cards with their wires dashed (DV-9 semantics). Virtual devices (DV-23) appear like hardware. | ✅ one card per device with ALL its connectors, in and out (Ui24R: 32 rows each way, measured on the real unit — `docs/devices/soundcraft-ui24r.md`); wired rows dark + "→ Channel", free rows grey; unplugged device = 55 % opacity — unplug/replug/greyed-with-wires-kept: `test_dv13_hotplug_new_multiport_device_is_listed_wired_replugged_and_persisted`; ports: `test_dv20_daemon_publishes_ports_of_a_device` |
| DV-27 | **Monitors column** (Loopback's "Show Monitors"): optional third/fourth column listing where the user listens (UX-2 listening device) with volume slider and jacks; hidden by default, one button shows it. | ✅ fifth patchbay column behind "Show Monitors" (page action): listening device (UX-2), one row per mix routed there with master slider (same cubic level as the mixer page) + live meter; hints when no device / no mix. Model: `patchbay().monitors` (AR-9). `test_dv27_monitors_column_is_hidden_by_default_and_shows_listening_device_with_its_mixes` via `--gesture monitors:on` + `--probe` |
| DV-14 | Device edges MUST have their own trim/mute inside kmixdeck; hardware volumes of the device itself MUST NOT be touched (Plasma owns them, CT-5). | ADR 0007 D5 | ✅ trim (cubic 0..1) + mute live ON THE WIRE (`DeviceRef.trim/muted`, persisted with the wire, re-applied when the edge node returns); bus `Channel.SetWireTrim/WireTrim/WireMuted` and the same on `Mix` (by ref); CLI `channel|mix wire <slug> <ref> [trim <level>] [mute on|off]`; patchbay: click on a device wire → popover with trim slider (dB), mute, remove. Hardware node volume never touched (CT-5). `test_dv14_wire_trim_and_mute_live_on_the_wire_not_on_the_device` (audio both directions, replug, restart), `test_dv14_wire_popover_shows_trim_and_mute_of_that_wire` |
| DV-15 | A mix MAY name a fallback output used while the primary is absent; without one the mix is silent, never rerouted to the default sink. | ADR 0007 D3; DV-3 | ✅ `Mix.FallbackOutput` (s), `kmixdeck mix fallback`, submenu in the header. test_mx9_mix_plays_to_several_outputs_and_dv15_fallback_takes_over::test_mx9_*: both outputs destroyed → fallback carries the mix, default sink stays silent, config kept; replug → primary wins, fallback quiet |
| DV-16 | On multi-channel hardware (RØDECaster, Ui24R) each usable slice MUST be turnable into its own named virtual device: own name, own icon, stereo pair or mono, chosen from the device's channel positions. The reference stays `node.name` + positions (DV-9/DV-13), so the slice survives replug and reboot. | owner 2026-09-15 | ✅ superseded by DV-17/18/19/21/25 (a slice = wires from named connectors into a named channel, or a mix onto a port subset); proven by `test_dv17_two_mono_channels_from_one_device_hear_only_their_port`, `test_dv18_mix_output_into_a_port_subset_leaves_other_ports_silent`, AR-10 hotplug test (survives replug + restart) |
| DV-7 | Virtual device identity (node.name) MUST stay stable across app updates so OBS/Discord keep their device selection (Wave Link L7: driver update changed device IDs). | wavelink L7; tests `test_dv7_levels_and_mute_survive_daemon_restart`, `test_dv7_state_is_keyed_by_stable_name_not_display_name` | ✅ |
| DV-28 | **Ui24R scale.** A 32-in / 32-out device (the Ui24R over USB, measured on a headless box with the Ui24R on USB) MUST be fully routable: every input port as a mono channel or side of a stereo channel, every output port as a mix target, second wires on any port, unknown ports refused; all edges up within seconds and back after a daemon restart. | Michel 2026-09-17 | ✅ 32 channels on AUX1..32 + mixes on AUX1+2 / AUX31+32 + side wire AUX22>R: edges < 10 s, restart < 15 s, audio on port 32 only in channel 32, unused output port silent. `test_dv28_ui24r_scale_32_in_32_out_every_port_routable_and_fast` |
| DV-29 | **A virtual device is a patch point, not two dead ends (Loopback parity, measured on the streaming host with the Ui24R 2026-09-18).** What an application plays into port N of a virtual device's *output* side MUST appear on port N of its *input* side (`kmixdeck.virt.<x>.out:playback_AUXn` → `kmixdeck.virt.<x>:capture_AUXn`), so a channel fed from that input hears the app — exactly how Loopback's virtual devices structure a rig ("Games" device: apps play into it, channels/mixes pick it up). Measured before: −24 dB into `.out:AUX3`, **−inf** at `:capture_AUX3` and at the channel wired to it. Implemented as one loopback per port pair inside the daemon (same mechanism as channel edges), created with the device and removed with it; per-port level MUST be visible on the patchbay card. | owner 2026-09-18 | ✅ `test_dv29_virtual_device_passes_apps_through_to_its_input_side`, `test_dv29b_pass_through_survives_a_daemon_restart_and_comes_from_config_alone`: .out:AUX3 → :capture_AUX3 → channel L only; other ports silent; survives restart; config fragment carries it. The input side IS the pass-through loopback's playback half (a separate null source targeted by a loopback got 0 links from WirePlumber — measured with every property variant); `node.async = true` on it breaks the scheduling cycle when a device is channel input AND mix return at once (without: every recorder in the subgraph returned 44-byte WAVs). Usable inputs = min(in, out); a device with `--out 0` keeps a standalone source (mic emulation) |
| DV-30 | **Rewiring a channel retargets the graph.** `channel input-remove` + `input-add` on the same channel MUST leave no stale edge: after the layout says `AUX3>L, AUX4>R` the PipeWire links MUST be `capture_AUX3 → wire0`, `capture_AUX4 → wire1` — measured 2026-09-18: layout updated, links still `capture_AUX2 → in`, `capture_AUX3 → w1.in` (audio arrived on the wrong side). Test: rewire, then assert the link table, then a tone on the new port reaches the new side and the old port reaches nothing. | owner 2026-09-18 (bug found on hardware) | ✅ `test_dv30_rewiring_a_channel_retargets_its_links`, `test_dv30b_fd_limit_lifted_and_a_failed_edge_is_reported_not_swallowed`, `test_dv30c_thirtytwo_by_thirtytwo_desk_never_loses_an_edge`. Root cause was not the rewire: EMFILE — 47 edges = 47 PipeWire clients ≈ 913 fds against a 1024 soft limit, the loopback's own client failed asynchronously ("Protocol error"), nothing in the registry, no error anywhere. Fix: `LimitNOFILE=65536` in the unit + `setrlimit` to the hard limit in main() + a 3 s edge watcher → `LastError` on the bus, `!!` line in `kmixdeck status`. Clamp test at 24 fds proves the report path; 32×32 desk test proves no edge is lost |
| DV-31 | **Device-native port numbering.** Hardware exposes its ports 0-based in PipeWire (`AUX0..AUX31` for the Ui24R), the mixer's own surface counts 1-based ("Aux 1/2 = Main"); virtual devices already count 1-based (`AUX1..`). Pickers, patchbay cards and `devices ports` MUST show the 1-based, human label alongside the PipeWire name ("USB 1 (AUX0)") and MUST accept both in refs; nothing else in the ref grammar changes. Without this the studio's Aux 1/2 is patched as AUX1/AUX2 and lands one channel off. | owner 2026-09-18 | ✅ `test_dv31_zero_based_hardware_gets_one_based_labels_and_refs_accept_both`. `devices ports` shows `AUX0  USB 1  capture_AUX0`; refs accept `USB 1` (CLI canonicalises to the PipeWire position before the bus, so the layout stays position-based, DV-9). Label rule: a device whose positions include `<letters>0` is 0-based hardware → "<letters> N+1" (AUX→USB); FL/FR and virtual `AUX1..` keep position = label. D-Bus `DevicePorts` entries gained a 4th field `label` |

## 5a. Architecture: service, CLI, frontends

| ID | Requirement | Source | Status |
|---|---|---|---|
| AR-1 | All mixer logic (layout, graph management, app routing, persistence, hotkey actions) MUST live in a background service (`kmixdeckd`) that runs without any UI. | owner 2026-09-14; test `test_ar1_cli_set_reaches_pipewire_and_is_audible` (CLI→bus→daemon→PipeWire, −12 dB measured) | ✅ |
| AR-2 | The service MUST expose its full functionality over a documented, versioned IPC API on the session bus, so that any desktop environment or third party can build a frontend without linking our code. The API spec (introspection XML) MUST ship in the repo and be the contract; the KDE UI MUST use only this API. | owner; tests `test_ar2_contract_matches_shipped_xml` (live introspection == interfaces/*.xml), `test_ar2_third_party_client_needs_none_of_our_code` (busctl only) | ✅ |
| AR-3 | A CLI (`kmixdeck`) MUST cover 100 % of the API: everything the UI can do, the CLI can do, scriptable, with `--json` output and stable exit codes. | owner; tests `test_ar3_cli_status_lists_the_prototype_graph`, `test_cli_level_syntax_and_exit_codes` — App routing/`watch` still to add; `kmixdeck app list|move` added; `watch` implemented | ✅ |
| AR-4 | The service MUST be D-Bus-activatable and run as a `systemd --user` unit bound to `pipewire.service` (`BindsTo=` + `After=`, `Restart=on-failure`), following `pipewire-pulse.service`. It MUST re-discover the graph after a PipeWire restart instead of dying. | platform; unit files `data/kmixdeckd.service.in`, `data/org.kmixdeck1.service.in` — PipeWire-restart reconnect not yet tested; reconnect with backoff on EPIPE, objects vanish/reappear on the bus; test `test_ch4_routing_survives_pipewire_restart` covers the daemon surviving a PipeWire restart | ✅ |
| AR-5 | The KDE UI MUST remain functional if started before the service (it activates it via D-Bus) and MUST show a clear state when the service is gone. | owner; test `test_ar5_frontend_call_activates_nothing_but_survives_daemon_gone` (exit 2, recovers); UI banner distinguishes service-gone vs PipeWire-gone | ✅ |
| AR-6 | Frontends MUST NOT need PipeWire access themselves; the service is the only PipeWire client. (Level meters are the exception to evaluate: see open question Q-6.) | owner; test `test_ar6_only_the_daemon_links_pipewire` (ldd) | ✅ |
| AR-7 | The repository MUST document how to write a frontend (docs/frontend-guide.md): bus name, object model, one worked example (the CLI). | owner; `docs/frontend-guide.md` | ✅ `docs/frontend-guide.md`; `test_ar7_frontend_guide_snippets_run_verbatim` executes the tour block line by line against the daemon and checks the effects (fader 0.25, muted, mix created, documented output "d 1" is true) |
| AR-8 | **Web UI (Michel 2026-09-16):** an optional browser frontend with the SAME capabilities as the KDE UI (mixer, patchbay, apps, FX, meters), served by a small HTTP/WebSocket bridge (`kmixdeck-web`) that speaks only the D-Bus API (AR-6 holds: no PipeWire in the bridge). Purpose: run kmixdeck on a headless/remote host (a headless box: Ui24R over USB, OBS) and drive it from any machine on the LAN. Auth: bind to localhost by default; LAN exposure behind a token. Meters over WebSocket at the daemon's 25 Hz. Bridge: `web/kmixdeck-web` (Python, Gio on the bus, `websockets`), one JSON view model, RFC 7386 patches, allowlist from the daemon's own introspection — ADR 0011. Proven: `test_ar8_bridge_serves_the_object_tree_and_writes_land_in_the_daemon`, `test_ar8_bridge_refuses_what_the_allowlist_does_not_name`, `test_ar8_lan_mode_needs_the_token_localhost_does_not`, `test_ar8_meters_arrive_over_the_websocket_at_the_daemon_rate`. UI: `web/static/` (vanilla ES modules, no build; mixer grid, apps, patchbay with drag-to-wire, meters, hearing bar, undo). Proven in headless Chrome: every CORE row of `test_core_feature_reaches_cli_window_and_tray` now has a browser column. Screenshots `docs/screenshots/web-*.png`. | ✅ |
| AR-9 | Web and KDE UI MUST share one JSON view model derived from the D-Bus object tree (the same names, the same refs), so a feature added to the daemon appears in both with UI work only. Documented in `docs/frontend-guide.md` (section "The browser frontend and the JSON view model"). Proven: `client.js` derives channels/mixes/cells from `state.objects` by interface + `ChannelOrder`/`MixOrder`; `patchbay.js: model()` mirrors `MixerClient::patchbay()` card ids, wire kinds and meter keys; `test_core_feature_reaches_cli_window_and_tray` asks window, tray and browser the same probe names. | ✅ |
| AR-10 | **Golden rule — every feature ships with its life-cycle proven (Michel 2026-09-16):** a feature or setting counts as done only when ONE integration test drives it through all of: (a) a device or app that appears from *outside* the daemon is recognised and usable at once, (b) unplug keeps the configuration and greys it out, (c) replug restores it *live* with **audio measured**, not only a property, (d) a daemon restart with the device present restores it, (e) a daemon restart with the device *absent* keeps the configuration and (f) plugging it in afterwards restores audio. No fixed sleeps for asynchronous state — wait for the observable. PRs that add a feature without this test are not mergeable. | Architecture | ✅ template test: `test_dv13_hotplug_new_multiport_device_is_listed_wired_replugged_and_persisted` (port-level wires through all six steps, audio measured on each side); rule spelled out in `CONTRIBUTING.md`; ctest is the gate (`ctest --output-on-failure` → 10/10) |
| AR-11 | **Backend and API first (rule 1, Michel 2026-09-17).** A feature exists when it is on the D-Bus tree with its interface XML and reachable from the CLI; no frontend holds state the daemon lacks. | ADR 0010 D1 | ✅ restates AR-1/AR-2 as a gate; enforced by `test_ar2_contract_matches_shipped_xml` + `tools/sot-audit.py` |
| AR-12 | **A feature is done only when every frontend we develop has it (rule 2, Michel 2026-09-17).** Each row carries a tier (core / full / api, ADR 0010 D3); `tools/sot-audit.py` refuses ✅ without a named proof for every frontend the tier demands (CLI, KDE window, tray). | ADR 0010 D3/D5 | ✅ `tools/sot-audit.py` refuses a core-tier ✅ without a row in the CORE table of tests/integration/frontends_sync suite; enforced by ctest `sot-audit`; 5 core rows proven CLI+window+tray (`test_core_feature_reaches_cli_window_and_tray`) |
| AR-13 | One client-side view model (`MixerClient`: `patchbay()`, `overview()`, maps) MUST be the only bus reader; KDE window, tray and web bridge render from it and never talk to D-Bus themselves. | ADR 0010 D2 | ✅ `MixerClient::overview()` + `patchbay()` are the only bus readers for window AND tray (same process, one client); web bridge will serialise the same structs. `test_tray_shows_only_what_overview_defines` |

## 6. UX

| ID | Requirement | Source | Status |
|---|---|---|---|
| UX-1 | Main view: channels as rows against mixes as panels — one fader per (channel, mix) cell, visible at once. | owner; Wave Link 3 layout (channel list left, one panel per mix right, horizontal faders that double as meters) | ✅ MixerPage (2026-09-15 redesign): ChannelHeader rows + one panel per mix, CellFader `[mute][fader/meter][link]`, MixHeader card (icon, name, output, master, meter); UI smoke test test_ct1_kde_frontend_registers_global_shortcuts starts it; screenshot-verified in Plasma (Breeze Dark) |
| UX-2 | A "what am I hearing" indicator MUST show which mix is currently routed to the user's headphones; switching MUST be one click. `tier:core` | wavelink | ✅ `Mixer.ListeningDevice` (layout, one truth) + "I hear:" bar on the mixer page (one click = that mix on my device); CLI `listen`; test `test_ux2_listening_device_is_one_truth`; observed on real hardware 2026-09-16 |
| UX-3 | First-run wizard SHOULD create default channels and mixes, detect the microphone and the default output, and assign running apps. | wavelink | ✅ Logic in the daemon: `Mixer.FirstRunPlan()` (default sink/source from WirePlumber's `default` metadata, running apps and where they would go by media.role) and `FirstRunApply()` (Monitor → default sink, listen there, Voice ← default mic as one port, unassigned apps → Game/System/Voice); `FirstRun` property = no layout.json when the daemon started. CLI `kmixdeck setup [--apply]`; window: dialog opens itself on first run, shows the plan in words, "Set up" calls Apply and lists what was done; also under the menu as "Set up defaults…". Refuses without a default output instead of doing half a job; idempotent on a second run (`test_ux3_first_run_wizard_wires_defaults_from_cli_and_window`). Screenshot docs/screenshots/first-run.png |
| UX-4 | Full keyboard operability and screen-reader labels per KDE HIG. | platform | ✅ Keyboard: every fader has focus (visible ring), ←/→ 1 dB, Shift 3 dB, PgUp/PgDn 6 dB, Home −∞, End/0 unity — in the fader's cubic scale, applied in the daemon; Tab reaches mute/link/dials/menus, Space toggles. Screen reader: every control we own carries `Accessible.name` after its channel and mix ("Game in Stream, slider, −3.0 dB"; "Mute Game in Stream"; "Trim of Voice"), verified through the accessibility tree (`--gesture a11y:`), Kirigami's own chrome excluded (`test_ux4_keyboard_drives_faders_and_mutes_and_every_control_has_a_screen_reader_name`). Found on the way: the trim/pan dials overflowed their row (labels under the next channel) — capped to the row height. Screenshot docs/screenshots/german-keyboard-focus.png |
| UX-5 | Languages: English first; German second; translatable via KDE's i18n. | owner | ✅ KI18n: `Messages.sh` + `tools/extract-messages.sh` → `po/kmixdeck.pot` (242 strings), `po/de/kmixdeck.po` complete (241 messages, plural forms, contexts), installed via `ki18n_install(po)`; QML gets the domain through `KLocalizedContext::setTranslationDomain` (without it every i18n() in QML silently stayed English). Test extracts fresh and fails on a stale .pot or an untranslated/fuzzy German string, checks %1 placeholders survive, and probes the window in `LANGUAGE=de` ("Ich höre:") (`test_ux5_german_catalog_covers_every_string_and_window_speaks_it`). CLI stays English on purpose (its output is parsed) |
| UX-6 | Level meters (VU) on every channel and every mix (top Linux wish: Sonusmix #20; Pulsemeeter has them). | users; ADR 0006: daemon peak streams (25 Hz, `resample.peaks`), `org.kmixdeck1.Levels` Subscribe/Peaks, on demand only; CLI `kmixdeck levels`; UI meters per cell (channel peak × gain) and per mix; tests `test_ux6_levels_signal_carries_peaks_of_the_tone`, `test_ux6_subscriber_that_dies_is_forgotten` | ✅ |
| UX-7 | Volume sliders MUST use a logarithmic curve and show dB and percent. | wavelink #19, #20 | ✅ CellFader/MixHeader: cubic slider (WirePlumber curve), tooltip/readout in dB, CLI accepts dB/%/linear (test_cli_level_syntax) |
| UX-8 | Every channel and every mix MUST have a user-chosen icon: a short curated set one click away, the full icon theme, or any image file. Presentation only — stored in the layout, never in PipeWire. | wavelink (channel/mix icons), owner | ✅ `Channel.Icon` / `Mix.Icon` (icon name or absolute path, "" = frontend default), persisted in `layout.json`, survives daemon restart; CLI `channel|mix icon <slug> <icon|none>`; UI: click the icon tile or "Icon…" in the menu → IconDialog (curated Breeze set, KIconDialog for the whole theme, file dialog). test_ux8_icon_is_persisted_and_survives_restart::test_ux8_* (3) Web: `test_dv1_web_input_picker_and_ux8_icon_from_the_browser`. |
| UX-9 | Channels and mixes MUST be reorderable; the order is part of the layout and survives restarts. Moving MUST NOT touch any fader, link or node. | wavelink (drag to reorder), owner | ✅ `Mixer.ChannelOrder` / `MixOrder` (as) + `MoveChannel/MoveMix(path, index)` (index clamped); CLI `channel|mix move <slug> <index|up|down|top|bottom>`; UI "Move up/down" (channels) and "Move left/right" (mixes) in the header menus. test_ux9_move_changes_order_only::test_ux9_* (5) |
| UX-10 | Every running application row MUST show the app's own icon and whether it is currently producing sound; apps on no channel MUST be visible as such so they can be picked up. | owner 2026-09-15 | ✅ AppsPage icon + running state; `test_ux10_app_row_shows_its_icon_and_running_state` (probe: icon source set, playing app ≥ 0.5 opacity, gone app silent or row removed); daemon side `test_ch10_running_apps_are_listed_with_their_channel` |
| UX-11 | Assigning an app to a channel MUST work by drag-and-drop onto the channel row (in addition to the picker); dropping onto several rows accumulates the assignment (CH-12), dropping somewhere else replaces it. | owner 2026-09-15 | ✅ drop = `assignApp(addOn)`; `test_ux11_drop_on_channel_row_assigns_the_app` via `--gesture drop:` — found and fixed 2026-09-16: a drop onto a second channel REPLACED the auto-routed first one instead of adding (Mixer::assignApp merged only with the layout entry, not with the live assignment) |
| UX-12 | Every entity (channel *and* mix) MUST have a listen button: press-and-hold routes exactly that entity to the main output, release restores the previous routing (solo audition). Works on a device with one built-in speaker as well as phones. | owner 2026-09-15 | ✅ `Mixer.Audition(o)` (`/` = release), `kmixdeck audition channel|mix <slug>|none`; hold button (headphones icon) on every channel/mix header; measured on real hardware: others muted while held, exact restore on release — `test_ux12_audition_mix_is_audible_and_release_restores`, `test_ux12_audition_channel_keeps_mixes_alive` (measured at the output) |
| UX-13 | Level meters on EVERY entity, not only channels and mixes: cells (channel × mix, post-fader), apps (who is talking right now — the running dot alone is not a level), inputs/devices and outputs. Same meter loop as UX-6: one subscription per visible node, 25 Hz, batched signal; invisible = not computed. | owner 2026-09-16 | ✅ Levels keys cell/, in/, out/, app/ next to channel/, mix/; meter streams capture output nodes directly; UI: cells, apps, mix output; test `test_ux13_every_entity_has_a_meter`; −12 dB cell measured on real hardware 2026-09-16 |
| UX-14 | Mixing MUST be possible from the KDE UI alone, end to end, without CLI: pick the monitoring output device (UX-2) and the stream output per mix, route apps to channels (UX-10/11), set faders, mute — first target: the laptop with RØDECaster + built-in speakers. Verified by a scripted UI walk-through on real hardware, screenshot as artefact. | owner 2026-09-16 ("Sodass ich auf dem Laptop echt schon mal mixen kann") | ✅ the whole walk-through through the UI's own handlers (`--gesture hear:/mixoutput:/drop:/fader:/mute:` drive the combo box, the Outputs menu entry, the drop target, the fader's onMoved, the mute button), CLI only reads; −12 dB set at the fader is measured at the mix, the chosen speakers carry signal. `test_ux14_mix_end_to_end_from_the_ui_alone`. Found on the way: a re-drop onto the same channel silenced it (relay destroy/create race — fixed), the UI could exit before its bus calls left under load (fixed) |
| UX-16 | Meters MUST read like a real meter, not like a scope: peak with **≥ 300 ms hold and a slow fall (≈ 20 dB/s)**, colour bands that do not flicker (green < −18 dBFS, amber < −6, red ≥ −6, with hysteresis), and all meters in one view updated from the **same daemon tick** so channel, cell, mix and output move together. A meter MUST NOT drop to zero between two ticks that both carry signal. Reference: EBU Tech 3205-E / IEC 60268-18 PPM ballistics. | owner 2026-09-16 (laptop: "zitterig, sofort rot, Stream-Fader fällt kurz auf null") | ✅ ballistics in the daemon (`Meters::publish`: instant rise, 8-tick hold, 0.8 dB/tick fall; atomic max-accumulate so a timer tick between two buffers no longer reads 0 — old daemon: 12/95 ticks at 0 with a steady tone), colour bands with 2 dB hysteresis in LevelMeter/Fader; test `test_ux16_meter_ballistics_no_dropouts_hold_and_slow_fall` (red on old daemon, green now) |
| UX-15 | A **routing view** MUST show the whole signal path as one picture: inputs/apps → channels → mixes → outputs/captures, with live levels on every edge (UX-13) and the same listen buttons (UX-12). Read-mostly: clicking an edge jumps to the fader that controls it. Not a second place to edit routing — the matrix and the Apps page stay the editors (no two truths). | owner 2026-09-15 ("Routing-Ansicht"), 2026-09-16 | ✅ `RoutingPage.qml`; the structure it draws (sources → channels → mixes → outputs, every edge = one daemon fact) is asserted by `test_dv24_patchbay_gestures_reach_the_daemon` on the shared model; live edge levels = UX-13 (`test_ux13_every_entity_has_a_meter`); listen buttons = UX-12 tests |
| UX-17 | **Tray icon.** A StatusNotifierItem MUST show a popover on click with the essentials from `overview()` (listening device, mixes with master + mute + meter, channels with mute + meter, running apps, daemon state); double-click MUST open (or raise) the full window. The tray never shows a feature that is not in `overview()`. | Michel 2026-09-17, ADR 0010 D4 | ✅ in `kmixdeck-kde` (KStatusNotifierItem, AR-6): click → `TrayOverview.qml` popover rendered from `overview()` (listening device, mixes with master/mute/meter, channels with mute/meter, running apps, service state); double-click → window raised; middle click → mute the listening mix; window close hides to tray. `test_tray_click_opens_overview_double_click_opens_window`, `test_tray_shows_only_what_overview_defines`, `test_core_feature_reaches_cli_window_and_tray` |
| UX-18 | **Loudness meter (EBU R128).** Every mix MAY show, next to its peak/RMS meter (UX-6/UX-16), integrated (I), short-term (S) and momentary (M) loudness in LUFS plus true-peak, computed in the daemon (`libebur128`) and published at meter rate over the existing meter path. Default **off** per mix (CPU + screen space), one toggle per mix, state persisted. The Stream mix SHOULD default it on. The target line (−14 LUFS for Twitch/YouTube, user-editable) MUST be drawn on the meter. `tier:full` | owner 2026-09-18 | 📝 |
## 7. Verification (binding)

| ID | Requirement | Source | Status |
|---|---|---|---|
| VF-1 | Every acoustic requirement (levels, mute, routing, persistence) MUST be asserted by an automated test that plays and records audio against a **private** PipeWire daemon — never the user's session. Method and rules: `docs/spec/testing.md`. | owner; pattern from PipeWire `pwtest` and plasma-pa | ✅ `tests/integration/pw_sandbox.py` (private PipeWire + WirePlumber + private D-Bus per suite); every acoustic test records/plays through it; `test_ar6_only_the_daemon_links_pipewire` |
| VF-2 | A requirement is marked ✅ only when a test in this repository asserts it; the test names the requirement ID, the requirement names the test. | owner | ✅ the rule; enforced by `tools/sot-audit.py` (ctest `sot-audit`): a ✅ row must name an existing `test_*` or its enforcement. First run 2026-09-16 found 11 violations, all resolved the same day |
| VF-3 | Measurements that justify an architecture decision MUST live in the ADR with a reproducible script/test, not in prose. | owner | ✅ rule; ADR 0002 carries `tools/` measurement scripts, ADR 0009 the port-fold measurements (`test_dv19_mono_input_reaches_both_sides_and_bad_port_is_rejected` documents the AUX3,AUX4>L refusal), `docs/devices/soundcraft-ui24r.md` the ALSA probe |
| VF-4 | `ctest` (unit + integration) MUST pass before merge; no sound card may be required to run it. | owner | ✅ |
| VF-5 | Audio measurements MUST use explicit port linking (`pw-link` by port name). `--target` auto-connect is forbidden in tests — it attached to the wrong port once and hid a real result. | lesson 2026-09-14 | ✅ rule; enforced by review: `grep -rn -- "--target" tests/` must stay empty (no test uses auto-connect); `pw_sandbox.play_into_port/record_port/level_at_port` link by port name |

| VF-7 | Capture sides of all kmixdeck loopbacks MUST be 1.0/unmuted after reconcile regardless of persisted state. | trap 5; test `test_vf7_capture_side_volume_is_healed_on_start` | ✅ |
| VF-6 | No kmixdeck output may ever be linked to a kmixdeck channel (feedback). Enforced by test on every graph change. | trap found 2026-09-14; test `test_no_feedback_loop_mix_outputs_never_target_a_channel` | ✅ |

## 8. Non-goals (for now)

- Windows/macOS ports.
- Being a DAW: no recording, no timeline.
- Replacing OBS's own mixer.
- Proprietary plugin formats (VST3 on Linux is possible but not a target for v1).
- Hardware-tied features (Wave XLR Pro hardware mixer, firmware updates, console connectivity) — we have no hardware.
- A marketplace. Presets are files; share them however you like.

## Open questions

- **Q-6 (level meters):** VU meters (UX-3) need audio samples. Options: (a) service computes peak/RMS per node and publishes at ~20 Hz over IPC, (b) frontends read `pw-stream` monitors themselves (breaks AR-6), (c) shared-memory ring. Decide with a measurement of D-Bus overhead at 20 Hz × (channels+mixes) properties.

Tracked as issues with label `question`. Initial list:

1. Should each mix expose a *virtual capture device* by default, or only on demand? (device clutter vs. convenience)
2. Per-channel *pan/balance* — needed?
3. How to present the (channel × mix) matrix on small windows.
