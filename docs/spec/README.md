# Specifications

- `requirements.md` — numbered, testable requirements with source tags.
- Architecture decisions live in `../adr/`.
- Raw research the requirements are derived from lives in `../research/`.

Change process: open an issue or PR against the spec. Every requirement has an
ID; reference it in commits and issues (`MX-2`, `CH-6`, …).
